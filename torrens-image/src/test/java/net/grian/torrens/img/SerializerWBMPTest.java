package net.grian.torrens.util.img;

import org.junit.Test;

import static org.junit.Assert.*;

public class SerializerWBMPTest {
    
    /*
    public void toStream() throws Exception {
        Texture texture = Texture.alloc(32, 32);
        byte[] bytes = new SerializerWBMP().toBytes(texture.toImage(false));
        assertFalse(bytes.length == 0);
    }
    */
    
}